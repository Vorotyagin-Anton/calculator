<!DOCTYPE html>
<html>
<head>
    <title>Calculator</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<div class="container" style="margin-top: 50px">
    {% if total is defined and total is numeric %}
        <h1>{{ postData.number1 }} {{ postData.operation }} {{ postData.number2 }} equals {{ total }}</h1>
    {% elseif postData.submit is defined %}
        <p style="color: red">'Numeric values are required'</p>
    {% endif %}

    <!-- Calculator form -->
    <form method="post" action="/">
        <input name="number1" type="text" value="{{ postData.number1 ?? '' }}" class="form-control" style="width: 150px; display: inline"/>
        <select name="operation">
            {% for operationName in ['Plus', 'Minus', 'Times', 'Divided By'] %}
                <option
                    value="{{ operationName }}"
                    {% if postData.operation ?? '' == operationName %}selected="selected"{% endif %}
                >
                    {{ operationName }}
                </option>
            {% endfor %}
        </select>
        <input name="number2" type="text" value="{{ postData.number2 ?? '' }}" class="form-control" style="width: 150px; display: inline"/>
        <input name="submit" type="submit" value="Calculate" class="btn btn-primary"/>
    </form>

</div>

</body>
</html>